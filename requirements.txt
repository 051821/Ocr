# --- core ---
requests
numpy
opencv-python
Pillow
python-dotenv
rapidfuzz

# --- CLIP classifier ---
torch
open_clip_torch

# --- PaddleOCR: GPU build ONLY (constraint) ---
# Install paddlepaddle-gpu matching your CUDA version FIRST, e.g. for CUDA 11.8:
#   pip install paddlepaddle-gpu==2.6.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
# Do NOT `pip install paddlepaddle` (CPU build) -- it will silently fall back to CPU.
paddleocr

# --- EC2 lifecycle (only needed if AUTO_MANAGE_EC2=true) ---
boto3
